---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section scroll-mt-20 w-full mx-auto px-4 py-16 sm:py-20 md:py-24 ${className || ''}`}
>
  <div class="max-w-4xl mx-auto">
    <slot />
  </div>
</section>
