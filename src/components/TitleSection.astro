---
const { title, subtitle } = Astro.props;
---

<div class="text-center mb-12">
  <h2 class="text-3xl sm:text-4xl font-bold text-white">{title}</h2>
  {subtitle && <p class="mt-2 text-gray-300 text-lg">{subtitle}</p>}
</div>
